const apiKey = {
  googleMaps: process.env.GOOGLE_MAP_API_KEY
};

const apiUrl = {
  googleMaps: "https://maps.googleapis.com/maps/"
};

const apiService = {
  googleMaps: "api/place/autocomplete/json"
};

export { apiKey, apiUrl, apiService };
